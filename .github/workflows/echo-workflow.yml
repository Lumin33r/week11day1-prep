name: Echo Workflow
on:
  push:
    branches:
      - main
pull_request:
    branches:
      - main
jobs:
  echo-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Echo message
        run: echo "Hello, this is an echo workflow!"
      - name: Multi line step
        run: |
            echo "This is a multi-line step."
            echo "You can add as many lines as you want."
            echo "Each line will be executed in order."
            pwd